# --------------------------
# 通用忽略规则（所有项目适用）
# --------------------------
# 依赖包文件夹（Node.js项目核心，无需提交）
node_modules/
# 环境变量文件（含敏感信息，必须忽略）
.env
.env.local
.env.development
.env.production
# 日志文件
logs/
*.log
# 编辑器/IDE配置文件（避免不同开发者的配置冲突）
.idea/        # WebStorm/IntelliJ
.vscode/      # VS Code
*.swp         # Vim临时文件
*.swo
.DS_Store     # Mac系统自动生成的隐藏文件

# --------------------------
# 前端项目（Vite+React）额外规则
# --------------------------
# 编译/构建产物（Vite打包后生成）
dist/


# --------------------------
# 后端项目（Node+Express）额外规则（如果后端是单独仓库）
# --------------------------
# 后端可能的构建产物（若用TypeScript，编译后生成的JS文件）
dist/
build/
# 数据库本地文件（若MySQL数据文件在项目目录下，需忽略）
mysql-data/  